# LINEチャネル設計（Phase 1 MVP）

**対象プロジェクト**: 水とでんきの救急センター
**位置づけ**: `docs/vision.md` / `docs/assumptions.md` / `docs/data_model_phase1.md` / `docs/business_workflow.md` / ADR-001 / ADR-002 と整合させる設計書。責任分界の正式な決定は `docs/adr/ADR-004-line-channel-responsibility.md` を参照。
**ステータス**: 設計確定・実装移行版（🔶は実装時に確定する軽微な残課題のみ）
**検討過程**: v0.1〜v0.3の議論（LIFF方式の採用、Customer/ChannelIdentity分離、障害時フォールバックの追加等）はGitのコミット履歴を参照。本ファイルは最新版のみを保持する。

---

## 1. LINEの役割と責任範囲

> LINEは「水とでんきの救急センター」における**顧客との受付・連絡チャネル**である。
> 顧客・案件・業者・ステータス等の業務データの正本は **Bubble側** で管理する。
> LINEは業務データベースとして利用しない。

将来的にはLINE以外の受付チャネル（Webフォーム・電話）も同じBubble基盤に集約する前提でデータモデルを組む（4章参照）。

---

## 2. 責任分界表

| システム | 主な責任 | 明示ルール |
| --- | --- | --- |
| **LINE** | 顧客との入口、通知の受信UI、リッチメニュー | 個人情報をトーク上に直接書かせない（11章） |
| **LIFF（Bubbleホスト）** | 修理依頼の入力フォーム | Bubbleページをそのまま利用し、Makeを介さず直接Bubbleへ送信 |
| **Bubble** | 顧客・案件・業者・ステータス等の業務データの正本、業務ロジック全般 | 緊急度判定・業者選定・ステータス遷移は必ずBubble側に置く |
| **Make.com** | Bubble→LINEの通知配信、軽量な連携 | **業務ロジックを書かない**。「運ぶ・つなぐ」役に限定 |
| **Messaging API** | LINEとBubble/Makeを繋ぐ接続層 | 通知送信・友だち追加イベント取得に使用 |
| **Webサイト** | SEO集客・サービス説明・LINE/電話/Webフォームへの導線 | 将来的な複数受付チャネルの入口 |

---

## 3. 修理依頼を受け付ける最小ユーザーフロー

**正常系**

```
友だち追加 → あいさつメッセージ → リッチメニュー「💧水まわり／⚡電気」タップ
   ↓
LIFFが起動 → Bubbleホストのフォームページを表示
   （LINE userId・選択カテゴリをLIFFコンテキストから自動引き渡し）
   ↓
フォーム入力（症状・場所・住所・氏名・電話番号・写真[任意]・緊急判定質問）
   ↓
送信 → Bubbleが直接 Repair Request を生成
   ↓
Bubble → Make → Messaging API 経由で「受付完了」をLINEに通知
```

**障害系（フォールバック）**

```
LIFF/Bubbleフォームが開けない、または送信エラーが発生した場合
   → あいさつメッセージ・フォーム画面の両方に電話番号を常時表示
   → 電話受付（人力の対応フロー）にフォールバック
```

緊急修理サービスである以上、「システムが使えません」で終わらせないことを設計原則とする。

**採用理由（LIFF方式）**：会話形式（Bot Q&A）は状態管理が必要で、入力チェック・必須項目管理が難しい。LIFFでBubbleのページを直接開けば、フォーム送信＝Bubbleへの直接書き込みとなり、Makeを介さない分、二重書き込みのリスクも減る。

🔶 **PoCタスク**：Bubble側でLIFF SDKを使ってLINE User Idを受け取れるかを検証する（LINE-03、10章参照）。案件登録機能は含めない最小スコープで実施し、成功すれば設計論争を終了して実装を進める。

---

## 4. Customer設計

**採用しない設計**：LINE userId をそのまま Customer の主キーにする

**採用する設計**：

```
Customer
  ├─ customer_id（Bubble内部の主キー）
  ├─ 氏名・住所・電話番号 等
  │
  └─ ChannelIdentity（1対多）
        ├─ channel = "LINE" / "Web" / "Phone" ...
        └─ channel_user_id（LINEの場合はuserId）
```

理由：将来Webフォームや電話受付が増えたとき、同一顧客を複数チャネルにまたがって名寄せできるようにするため。Bubbleではテーブル追加のコスト自体が低いため、構造は最初から用意する。

**MVPでの簡素化方針**：
- 「友だち追加のみ」と「フォーム送信済み」を厳密に区別するステータス設計は行わない
- 電話番号による高度な名寄せロジックは実装しない
- ChannelIdentityには `channel` と `channel_user_id` のみを持たせる

将来チャネルが増えた段階で必要な状態管理を追加する。

---

## 5. Bubbleへ渡すデータ項目

| 項目 | 必須/任意 | 備考 |
| --- | --- | --- |
| LINE userId → channel_user_id | 必須 | ChannelIdentity生成 |
| カテゴリ（水まわり／電気） | 必須 | リッチメニューの選択と連動 |
| 症状テキスト | 必須 | 自由記述 |
| 発生場所 | 必須 | 例：キッチン、風呂場、分電盤 |
| 住所 | 必須 | 🔶 番地までの必須化粒度は要確認 |
| お名前 | 必須 | 電話連絡時の宛先確認のため |
| 電話番号 | 必須 | 折り返し連絡用 |
| 緊急判定質問（Yes/No） | 必須 | 「水が止まらない」「焦げ臭い」「ブレーカーが落ち続ける」等。回答結果から `urgency = HIGH/NORMAL` をBubble側で自動算出、顧客の自己申告ラベルには依存しない |
| 写真 | 任意 | Bubbleのファイルアップロード機能を利用 |
| 個人情報取り扱いへの同意 | 必須 | 11章参照 |

---

## 6. Bubbleから返す通知

| タイミング | 内容 |
| --- | --- |
| 案件受付直後 | 受付完了メッセージ |
| 業者確定時 | 担当業者名・到着予定 |
| ステータス更新時 | 対応状況 |
| 完了時 | 完了報告 |
| エスカレーション時 | 🔶 Option E確定後に設計 |

冪等性については12章を参照。

---

## 7. リッチメニュー（MVP）

```
┌──────────┬──────────┐
│ 💧 水まわり │ ⚡ 電気    │
└──────────┴──────────┘
```

「対応状況を確認」はMVPから除外（確認用の画面・APIが別途必要になるため後回し）。電話フォールバックはリッチメニューではなく、あいさつメッセージ・フォーム内に常時表示する形で対応する。

---

## 8. あいさつメッセージMVP案

```
友だち追加ありがとうございます！
「水とでんきの救急センター」です。

水漏れ・電気トラブルなど、お困りの際は
下部メニューから症状・住所を送るだけで、
対応可能な提携業者を手配いたします。

つながらない場合は下記お電話番号までご連絡ください。

【対応エリア】🔶要記載
【受付時間】🔶要記載
【お電話】🔶要記載
```

---

## 9. 現時点では実装しない機能

- キーワード解析による自動応答の高度化
- 複雑な多階層リッチメニュー
- LINE認証済みアカウントの申請
- 加盟店へのLINE管理画面アクセス権限付与
- AIによる高度な症状判定・自動見積
- クーポン・セグメント配信・顧客スコアリング

---

## 10. 実装WBS

| ID | タスク | 備考 |
| --- | --- | --- |
| LINE-01 | LINE公式アカウント開設 | 篠さん作業、並行実施可 |
| LINE-02 | Messaging API設定 | チャネルアクセストークン発行 |
| LINE-03 | **LIFF + Bubble PoC**（最優先） | LINE User IdをBubble画面に表示できるかのみ検証。案件登録は含めない |
| LINE-04 | Customer / ChannelIdentity実装 | 簡素化版（4章） |
| LINE-05 | 修理依頼フォーム（LIFF/Bubbleページ） | 氏名必須化を反映 |
| LINE-06 | Repair Request生成ロジック | Webhook冪等性・重複防止を含む |
| LINE-07 | 写真アップロード | 任意項目 |
| LINE-08 | 緊急判定ロジック | Yes/No質問からurgency算出 |
| LINE-09 | 受付完了LINE通知（Make経由） | |
| LINE-10 | 障害時フォールバック表示 | あいさつメッセージ・フォームに電話番号常時表示 |
| LINE-11 | E2Eテスト | 下記マイルストーン参照 |

**最初のマイルストーン**：LINE-03のPoCが成功した時点で設計論争を終了し、LINE-04以降の実装に進む。PoCが失敗した場合のみ、設計を見直す。

**最終ゴール（E2Eテスト）**：「LINEから水漏れ案件を1件送信すると、BubbleにRepair Requestが1件だけ生成される」ことを確認する。

---

## 11. 個人情報の取り扱い

フォームで氏名・電話番号・住所・写真を取得するため、以下が必要：
- フォーム内に「個人情報の取り扱いについて」へのリンクと同意チェックボックスを設置
- 🔶 プライバシーポリシー文書自体がまだ存在しないため、簡易版を作成する必要がある（個人事業主として最低限の記載で可、本格的な法務レビューはPhase2以降でも可）
- LINEトーク上には個人情報を直接書かせない設計を徹底する（LIFFフォームへ誘導する理由の一つ）

---

## 12. Webhookの冪等性・重複防止設計

LINE Webhook／Make／Bubble間の連携で、タイムアウトや再送により同一依頼が二重に案件化されるリスクがある。

**対応方針**
- LINEのWebhookイベントには一意の `webhookEventId` が付与されるため、これをBubble側で一時的に記録し、同一IDの重複処理を弾く
- フォーム送信（LIFF→Bubble直接）の場合も、短時間（例：数十秒〜数分）以内の同一顧客・同一内容の重複送信をBubble側でチェックする軽量ロジックを入れる
- 🔶 具体的な重複判定ロジック（時間窓・比較項目）はBubble実装時に確定

---

## 未解決事項サマリー（🔶一覧）

1. 住所の必須項目粒度
2. AI（緊急度判定・分類）をPhase1で使うか（現時点では見送り、Phase2以降で再検討）
3. エスカレーション時の通知文言（Option E確定待ち）
4. 対応エリア・受付時間・電話番号の記載内容
5. プライバシーポリシー文書の作成
6. 重複判定ロジックの具体設計（時間窓・比較項目）

これらは既存の「4つの未解決業務課題」と一部重なるため、業務設計トラックと合流させて解決する。実装を止める理由にはならないため、LINE-03のPoCと並行して詰めればよい。
